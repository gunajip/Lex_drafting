# LexDraft Assist — Python dependencies
# Install with:  pip install -r requirements.txt

# PDF parsing
pymupdf>=1.24.0

# Vector database
chromadb>=0.5.0

# Ollama Python SDK (optional — server falls back to urllib if absent)
ollama>=0.2.0
